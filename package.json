{
	"name": "homerunner-cloudflare-cache",
	"version": "1.0.3",
	"private": true,
	"scripts": {
		"watch": "sass assets/sass/:./assets/css/ --style expanded --watch",
		"css": "sass assets/sass/:./assets/css/ --no-source-map --style expanded || exit 0",
		"build": "grunt build"
	},
	"main": "Gruntfile.js",
	"devDependencies": {
		"grunt": "^1.6.1",
		"grunt-contrib-compress": "^2.0.0",
		"grunt-string-replace": "^1.2.1",
		"matchdep": "^2.0.0",
		"sass": "^1.79.4"
	}
}